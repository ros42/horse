# 🐎 Horse Trainer - Спортивный тренажёр "Конь"

Приложение для управления спортивным тренажёром на базе двух сервоприводов LICHUAN A5 Series.
Имитирует движения лошади (шаг, галоп) для тренировки спортивных навыков.

## Возможности

- 🔌 Подключение к двум сервоприводам через Modbus RTU (COM-порт)
- 📊 Мониторинг регистров в реальном времени
- 🎮 Ручной режим управления каждым приводом
- 🔄 Автоматический режим с синхронизированными циклами
- ⚙️ Настраиваемые паттерны движения (шаг, галоп)

## Установка

```bash
# Создать виртуальное окружение
python -m venv venv

# Активировать (Windows)
venv\Scripts\activate

# Установить зависимости
pip install -r requirements.txt
```

## Запуск

```bash
python src/main.py
```

## Архитектура

```
horse/
├── src/
│   ├── main.py              # Точка входа
│   ├── modbus_manager.py    # Управление Modbus соединениями
│   ├── servo_device.py      # Класс сервопривода A5
│   ├── motion_controller.py # Контроллер движения (алгоритмы)
│   └── ui/
│       ├── main_window.py   # Главное окно
│       ├── servo_panel.py   # Панель управления сервоприводом
│       ├── motion_panel.py  # Панель режимов движения
│       └── settings_dialog.py # Диалог настроек
├── config/
│   └── settings.json        # Конфигурация
├── requirements.txt
└── README.md
```

## Оборудование

- **Сервоприводы**: LICHUAN A5 Series AC Servo Drive
- **Протокол**: Modbus RTU через RS485
- **Функции Modbus**: 0x03 (чтение), 0x06 (запись одного), 0x10 (запись нескольких)

## Лицензия

MIT

